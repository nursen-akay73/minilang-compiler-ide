// PDF şartnamesindeki örnek program (yorum satırı testi)
int x;
int y;
float result;
x = 10;
y = 3;
result = x + y * 2;
if (result > 15) {
    print("Result is large");
} else {
    print("Result is small");
}
while (x > 0) {
    x = x - 1;
}
